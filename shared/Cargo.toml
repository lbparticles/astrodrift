[package]
name = "shared"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
libm = "0.2.11"

[features]
default = []
cuda-oxide = []

[dev-dependencies]
rstest = "0.26.1"
approx = "0.5.1"
rand = "0.9.2"

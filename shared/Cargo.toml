[package]
name = "shared"
version = "0.1.0"
edition = "2024"

[dependencies]
cust_core = { git = "https://github.com/Rust-GPU/Rust-CUDA", package = "cust_core", branch = "main" }
libm = "0.2.11"

# [lib]
# crate-type = ["cdylib","rlib"]
